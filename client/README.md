# 🏡 EstateFlow – Real Estate Platform (Frontend)

🧠 **Client Repo**: [GitHub]()



## 🚀 Setup Instructions

```bash
git clone <repository-url>
cd client
npm install
npm run dev

```
## 🔐 Environment Variables

Add a `.env` file in the `client` directory with:

```env
VITE_BASE_URL=http://localhost:5000
VITE_STRIPE_PAYMENT_PUBLISHABLE_KEY=your_stripe_publishable_key
```

### Required Variables:
- `VITE_BASE_URL` - Backend API URL (default: http://localhost:5000)

